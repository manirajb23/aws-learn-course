title: AWS Learn
description: 'learning beginner  '
template:
  repo: aws-learn-template
  name: aws-learn
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
